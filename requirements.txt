# Core dependencies
# Note: In a real implementation, we would use libimobiledevice
# and related libraries for iOS device communication

# UI dependencies
prompt_toolkit>=3.0.0
colorama>=0.4.4

# Development dependencies
pytest>=7.0.0
black>=22.0.0
mypy>=0.9.0
